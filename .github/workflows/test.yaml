name: test
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: test
        run: |
          composer update
          vendor/bin/phpunit tests
      - name: composer
        run: |
          composer update
      - name: test
        run: |
          vendor/bin/phpunit

